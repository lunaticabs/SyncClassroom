[workspace]
members = [
    "apps/teacher/src-tauri",
    "apps/student/src-tauri",
]
resolver = "2"

# 共享 profile，统一优化配置
[profile.release]
opt-level    = 3
lto          = true
codegen-units = 1
strip        = true

[profile.dev]
opt-level = 0
